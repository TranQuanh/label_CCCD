# CCCD VLM Extraction
# ==========================================
#   Gán nhãn (ground truth): Qwen2.5-VL-7B-Instruct
#   Fine-tune / Deploy      : Qwen3-VL-8B-Instruct  (QLoRA 4-bit)
#   pip install -r requirements.txt

# ── Runtime for backend (proxy mode) ─────────────────────────────────────
# Only needed when running backend and frontend, calling model via ngrok.
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
psycopg[binary]>=3.1
psycopg-pool>=3.1
redis>=5.0.0
pyjwt>=2.8.0
bcrypt>=4.0.0
requests>=2.25.0   # for calling ngrok-exposed model

# ── Optional packages (uncomment if you need labeling, training, or local inference) ──
# torch>=2.1.0
# torchvision>=0.16.0
# accelerate>=0.27.0
# Pillow>=10.0.0
# tqdm>=4.66.0
# transformers>=4.49.0
# qwen-vl-utils>=0.0.8
# peft>=0.10.0
# bitsandbytes>=0.43.0
# gradio>=4.0.0
# pyngrok>=7.0.0   # if you want to expose local backend via ngrok
